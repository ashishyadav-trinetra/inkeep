# Railway config for SpiceDB (authorization service)
# In Railway UI → Service Settings → Source → set "Config Path" to: railway/spicedb.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.spicedb"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
